defmodule CrossBorderWeb.PageController do
  use CrossBorderWeb, :controller

  def index(conn, _params) do
    # conn
    # |> put_layout({CrossBorderWeb.LayoutView, "auth.html"})
    # |> render("index.html")
  end
end
